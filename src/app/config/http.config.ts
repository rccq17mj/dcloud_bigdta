// 地址前缀
const API_PREFIX = '';

// 所有的请求地址放在这里
export const HttpApiUrls = {
  /**
   * 示例
   */
  example: {
    getUser: API_PREFIX + '/user/profile',
    getMenus: API_PREFIX + '/user/menu',
  }
};
